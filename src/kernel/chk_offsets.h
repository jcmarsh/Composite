/* At init, should check to make sure that we have defined the correct offsets into various
 * structures. Unfortunately we can not use offsetof to do this at compile time
 */

void check_offsets(void);
